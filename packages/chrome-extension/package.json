{
  "name": "contextweaver-chrome",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension component for ContextWeaver.",
  "scripts": {
    "build": "tsc -p ./ && echo 'CE webpack/esbuild placeholder'",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.200",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "@typescript-eslint/parser": "^5.0.0",
    "@typescript-eslint/eslint-plugin": "^5.0.0"
  }
}