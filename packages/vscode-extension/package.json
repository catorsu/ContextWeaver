{
  "name": "contextweaver-vscode",
  "displayName": "ContextWeaver VSCode",
  "description": "VS Code component for ContextWeaver, providing project context to the Chrome extension.",
  "version": "0.0.1",
  "publisher": "YourPublisherName",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "contextweaver.helloWorld",
        "title": "ContextWeaver: Hello World"
      }
    ],
    "configuration": {
      "title": "ContextWeaver",
      "properties": {
        "contextweaver.ipc.port": {
          "type": "number",
          "default": 30001,
          "description": "Port for IPC communication with the ContextWeaver Chrome Extension."
        },
        "contextweaver.ipc.token": {
          "type": "string",
          "default": "",
          "description": "Shared secret token for IPC authentication. Must match the token in the Chrome Extension."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "dependencies": {
    "ws": "^8.17.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@types/ws": "^8.5.10",
    "@types/uuid": "^9.0.8",
    "@types/node": "18.x",
    "typescript": "^5.3.3",
    "eslint": "^8.56.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "vscode-test": "^1.5.0"
  }
}